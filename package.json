{
  "name": "iron-student-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Industry-style student portal built with React, Vite, Express, secure demo auth, and API-ready backend adapters.",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace client\" \"npm run dev --workspace server\"",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server",
    "install:all": "npm install --workspaces"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
